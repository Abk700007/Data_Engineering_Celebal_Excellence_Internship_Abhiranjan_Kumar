-- CREATE DATABASE sql script

-- Drop database if it exists to allow clean re-runs
-- DROP DATABASE IF EXISTS ecommerce_sales;

-- Create the database
CREATE DATABASE ecommerce_sales;

-- Note: Use the newly created database (RDBMS-specific command like 'USE ecommerce_sales;' in MySQL)
